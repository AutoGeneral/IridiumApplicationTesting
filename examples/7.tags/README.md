This is an example using tags on Scenarios.

The important settings here are these settings in the configuration.xml file:

```
    <property name="javaws.tagsOverride" value="@PAGEOBJECT,@LAUNCH,@POSTCODE"/>
```